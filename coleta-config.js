/* coleta-config.js — LME
   URL vazia = MODO OFFLINE: as paginas funcionam e exportam JSON localmente, sem enviar nada
   (briefing PRE-site-lme-oa, decisao 8 — como na ILB). Quando a planilha e o Apps Script
   existirem (alvo Minhas Entregas), preencher COLETA_URL com a URL /exec e republicar.
   O contrato e o mesmo da FC1, da FE1 e da ILB. */
/* contrato canonico do ecossistema: o campo e `url` (igual em FC1 e FE1) */
window.COLETA = { url: "" };
